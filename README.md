# Using these files

Create a **constants.py** file

Add the following lines:

SIMULATION_PATH = "path/to/your/sim/info_XXXXX.txt"

SILMARIL_PATH = "local/install/of/silmaril"

MERLIN_PATH = "local/install/of/Merlin" # used for lum_sum.ipynb
